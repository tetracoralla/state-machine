interface:
  display_name: "Step Switch"
  short_description: "Run deterministic state transitions and simulations."
  default_prompt: "Use $use-state-machine to simulate this event sequence."

dependencies:
  tools:
    - type: "mcp"
      value: "state_machine"
      description: "Read-only finite-state validation and simulation tools"
