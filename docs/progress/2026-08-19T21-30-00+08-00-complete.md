---
timestamp: 2026-08-19T21:30:00+08:00
segment: complete local build
status: complete
source: codex
---

# State Machine local build closeout

Delivered artifacts:

- One versioned finite-state Machine Spec and deterministic TypeScript kernel.
- Strict validation, single-step execution, bounded simulation, path finding,
  inspection, and semantic diff operations.
- CLI, six read-only MCP tools, bundled repo-local Codex plugin and thin Skill.
- Browser YAML editor, validation feedback, topology, path finder, simulator,
  trace, symbolic effects, import/export, draft persistence, and recovery.
- Generated JSON Schemas, order example, Apache-2.0 distribution files,
  security policy, review contract, and rerunnable checks.
- Clean targets are contained below the canonical project root and reject a
  symbolic link in any existing path component; the escape case has a
  negative regression.

Current validation:

- `npm run check`: PASS; 18 source tests and 10 built-runtime tests passed.
- Generated schema drift, contract checks, plugin checks, type checks, node/UI
  builds, built CLI, and bundled MCP stdio probes: PASS.
- Official local plugin validator and Skill validator: PASS.
- `npm pack --dry-run --json`: PASS; about 725 KB packed.
- Built browser: allowed transition and final-state flow PASS; invalid-source
  pause, undo/reload recovery, guarded pass/reject, path display, and narrow,
  medium, and wide rendered review PASS; console reported zero messages.

Remaining acceptance:

- Installed-host activation was not performed because the repository must not
  modify a personal plugin marketplace without an explicit installation request.
- Business and visual acceptance remains the owner's judgment against the
  current rendered interface.

Blockers:

- None for the local project build.
