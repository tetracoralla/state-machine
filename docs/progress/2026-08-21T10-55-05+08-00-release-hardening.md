---
timestamp: 2026-08-21T10:55:05+08:00
segment: GitHub-first release hardening
status: local candidate complete
source: codex
---

# State Machine release-hardening closeout

Delivered in this segment:

- GitHub-first product identity, repository marketplace, install contract,
  release process, changelog, contribution guide, CI, and dependency updates.
- The Node package remains private; plugin users consume the committed prebuilt
  server and do not require npm or a build step.
- A package gate creates a real tarball, excludes source maps and internal
  progress records, caps unpacked size, installs into an isolated consumer, and
  executes the installed CLI.
- Contract gates pin marketplace, manifest, repository, private-package, install
  command, registry, generated-schema, and bundled-server invariants.
- Responsive workbench modes now stack the definition and topology before they
  become cramped, preserve wide-screen columns, expose scroll affordances, and
  keep page, source, and narrow-topology scrolling operable with real wheel
  input.

Current validation:

- `npm run check`: PASS; 34 source tests, 16 built-runtime tests, and 12 browser
  tests passed.
- Package check: PASS; 320,523 bytes packed and 1,477,195 bytes unpacked, with
  the isolated installed CLI returning the shipped machine as valid.
- Built CLI accepted and guard-rejected transitions: PASS.
- Bundled MCP stdio focused run: PASS; 10 tests including discovery, calls,
  closed-world rejection, and request limits.
- Official npm registry audit at moderate threshold: PASS; zero vulnerabilities.
- Visible browser review: PASS at 390, 900, 1200, and 1440 pixels; no document
  horizontal overflow, 900/1200 use stacked editor topology, 1440 uses columns,
  and 1200 page plus 700 topology wheel scrolling changed real scroll position.
- Browser console: zero errors and zero warnings.

External and owner lanes:

- No commit, remote repository, push, tag, GitHub release, universal-directory
  submission, npm publication, or personal Codex installation was performed.
- Installed-host activation remains pending until the candidate is available
  from an owner-authorized GitHub tag and may not be inferred from source or
  stdio success.
- Business and visual acceptance remains the owner's judgment against the
  current rendered interface.

Next authorized action:

- Review the diff and current interface. If accepted, authorize the external
  GitHub commit/push/tag/release sequence and tagged installed-host validation.
